# Git will handle the files in whatever way it thinks is best. This is a good default option.
# https://help.github.com/articles/dealing-with-line-endings/
* text=auto

# Do not adjust line endings on Sitecore .item files.
# http://seankearney.com/post/Using-Team-Development-for-Sitecore-with-GitHub
*.item -text

# Explicitly declare text files you want to always be normalize
# and converted to native line endings on checkout.
*.cs

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.jpg binary
*.png binary
