@using Sitecore.Forms.Mvc.Html
@using Sitecore.Forms.Mvc.ViewModels.Fields
@model DropListField

@using (Html.BeginField())
{
    @FormFieldWithToolTip.Create(Model.Information, Html.DropDownListFor(x => Model.Value, Model.Items, new { @class = "form-control" }))
    // Make ShowInformation to false as default behaviour is to render information below the field
    Model.ShowInformation = false;
}
