import { Image } from "./../image";

export class Permutation{
    coreModules : Array<string>;
    crossIcon: Image;
    externalIdentifier: string;
    id :number;
    perMonthText : string;
    tickIcon : Image;
    title : string;
}
